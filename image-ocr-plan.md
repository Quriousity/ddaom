# 그림 안 글자도 상자로 — 설계 (2026-08-05)

> **상태: 설계만. 구현하지 않았다.**
> 대상 = [extractor.py](app/core/extractor.py), [main_window.py](app/ui/main_window.py),
> [pdf_view.py](app/ui/pdf_view.py).

## 1. 문제

[`scan_page_boxes()`](app/core/extractor.py:124) 는 **페이지 단위로 전부 아니면 전무**다:

```python
    if boxes:                      # 글자층 줄이 하나라도 있으면
        return boxes, "text-layer" # 여기서 끝. 아래 OCR 은 안 탄다
```

본문 글자가 있는 페이지에서는 그림 안에 구워진 글자를 쳐다보지도 않는다.
실제 문서(`SW55 A Deployment & Maintenance Guide`, 14쪽)에서 확인:

- **14쪽 전부**가 글자층+이미지 혼재 (이미지 전용 페이지 0개) → 어느 페이지에서도 그림 라벨은 상자가 안 생긴다
- 1쪽: 글자층 35줄, 그림 4개, **그림 영역과 겹치는 글자층 줄 0개** — 라벨은 순수 래스터다
- 같은 페이지에 OCR 을 강제로 돌리면 79줄, 라벨 점수 **0.98~1.00**

OCR 성능 문제가 아니다. 한국어 rec 모델이 영어 라벨을 못 읽는 것도 아니고
(`SCAN_DPI=200` vs 그림 원본 ~220dpi 로 해상도도 충분하다), **OCR 이 실행되지 않을 뿐이다.**

드래그 선택은 지금도 된다 — [`extract_text()`](app/core/extractor.py:159) 는 **선택 영역마다**
판단하므로 라벨 위를 고르면 글자층이 비어 OCR 폴백으로 넘어간다 (0.85초, 정확).
즉 "글자를 못 읽는" 게 아니라 **"클릭할 상자가 안 생기는"** 문제다.

## 2. 실측 — 이 결과가 원래 발상을 뒤집는다

발상은 "그림 영역만 잘라 OCR 하면 싸게 먹힌다" 였다. 전 페이지를 재어 봤다
(그림 = 페이지 면적의 1% 이상인 이미지, `SCAN_DPI=200`):

| 쪽 | 그림 | 면적비 | 글자층 | 그림만 OCR | 전체 OCR |
|---:|---:|---:|---:|---:|---:|
| 1 | 4 | 22% | 35줄 | 3.03초 | 6.76초 |
| 2 | 1 | 1% | 47줄 | 0.50초 | 4.38초 |
| 3 | 0 | 0% | 35줄 | **0.00초** | 5.15초 |
| 4 | 0 | 0% | 71줄 | **0.00초** | 5.52초 |
| 5 | 8 | 37% | 15줄 | 8.23초 | *5.93초* |
| 6 | 5 | 26% | 20줄 | 5.31초 | *4.35초* |
| 7 | 8 | 35% | 22줄 | 7.89초 | *6.23초* |
| 8 | 8 | 36% | 19줄 | 7.22초 | *5.61초* |
| 9 | 4 | 19% | 41줄 | 6.15초 | *5.63초* |
| 10 | 8 | 38% | 20줄 | 11.52초 | 13.38초 |
| 11 | 6 | 28% | 26줄 | 8.04초 | *6.02초* |
| 12 | 6 | 28% | 25줄 | 7.05초 | *5.76초* |
| 13 | 8 | 38% | 18줄 | 4.21초 | *3.42초* |
| 14 | 4 | 19% | 4줄 | 1.95초 | *2.43초* |
| | | | **합계** | **71.1초** | **80.6초** |

**그림만 잘라 돌려도 전체의 88% 다.** 기울임 표시한 8개 페이지에서는 오히려 **더 느리다.**
페이지 면적의 3분의 1만 훑는데 시간은 그대로인 이유는, 비용이 픽셀 수가 아니라
**호출 횟수**에 붙기 때문이다 — 그림 8개를 8번 부르면 det 전처리·모델 입력 정규화·
rec 배치 구성을 8번 치른다. 큰 이미지 1장이 작은 이미지 8장보다 싸다.

**따라서 "그림만 잘라 OCR" 은 비용 근거가 없다.** 다만 실측이 확실히 보여주는 이득이
하나 있다: **그림이 없는 페이지는 아예 안 돌린다** (3·4쪽 5.15·5.52초 → 0초).

> **결론: 실행은 페이지 1회로 하고, 채택만 그림 영역으로 제한한다.**
> 사용자가 얻는 것("그림 안 라벨이 상자가 된다")은 같고, 비용은 더 싸다.

## 3. 설계

### 3.1 2단계 반환

지금 스캔은 이미 백그라운드 워커다 ([main_window.py:501](app/ui/main_window.py:501)).
UI 는 멈추지 않으므로 문제는 "느림"이 아니라 "상자가 늦게 뜸"이고, 단계를 나누면 된다.

| 단계 | 하는 일 | 비용 |
|---|---|---|
| 1 | 글자층 상자를 그대로 반환 — 지금과 동일 | 0.00초 |
| 2 | 그림이 있으면 페이지를 1회 OCR, **그림 영역 안 줄만** 상자로 추가 | 2~13초 |

1단계 상자가 즉시 뜨고, 2단계 상자가 나중에 얹힌다. 그림이 없는 페이지는 2단계가
그림 목록을 보고 즉시 빈손으로 끝난다.

### 3.2 채택 규칙

OCR 이 뱉은 줄 중 상자로 승격할 것:

1. **그림 안에 있다** — 줄 폴리곤의 **중심**이 그림 rect 안. 중심점 판정은
   [`_leftover_text`](app/core/redactor.py:315)·[`extract_text`](app/core/extractor.py:184) 와 같은 규칙이다.
   스치기만 한 본문 줄을 끌고 들어오지 않는다.
2. **그림이 충분히 크다** — rect 면적 ≥ 페이지의 1%. 실측상 이 기준이 걸러내는 것은
   머리글 로고(195×51px)와 본문 중간 아이콘들이다. 2쪽의 79×61pt 아이콘 4개가
   여기서 빠지는데, 그 안에는 글자가 없다.
3. **글자층 상자와 겹치지 않는다** — 겹치면 버린다. 글자층 쪽이 언제나 더 정확하다.

### 3.3 함수 모양

`scan_page_boxes()` 를 쪼갠다. 스캔본(글자층 없는 문서) 경로는 그대로 둔다 —
그건 이미 페이지 전체 OCR 이고 잘 동작한다.

```
scan_page_boxes(doc, page_no)        # 그대로. 글자층 우선, 없으면 전체 OCR
scan_image_boxes(doc, page_no)       # 새로 추가. 그림 영역 안 OCR 줄만
    → 그림 없으면 즉시 []            # 페이지 넘김 비용 0
```

`main_window._scan_page()` 는 워커를 둘 띄우고, 2단계 결과는 **덧붙인다**.
지금 [`set_page_boxes()`](app/ui/pdf_view.py:91) 는 목록을 **교체**하므로 그대로 쓰면
1단계 상자가 날아간다 — 추가용 경로가 필요하다.

재스캔은 걱정 없다: [`_request_scan()`](app/ui/pdf_view.py:88) 이 `text_boxes` 캐시를 보고
페이지당 한 번만 요청한다.

### 3.4 상태 표시

2단계가 도는 동안 `"그림 속 글자 찾는 중…"` 을 [`_set_ocr_status`](app/ui/main_window.py:499) 로
보인다. 10초 넘게 아무 말 없이 상자가 늘어나면 사용자는 앱이 이상하다고 생각한다.

## 4. 위험

| 위험 | 내용 |
|---|---|
| **파괴 대상이 된다** | 상자 클릭 = 담기 = **파괴 목록**이다. OCR 이 잘못 그린 폴리곤을 클릭하면 엉뚱한 범위가 파괴 대상이 된다. 글자층 상자와 신뢰도가 다르므로 **표시를 구분할지** 정해야 한다 |
| **회전 페이지** | `get_image_rects()` 의 좌표계가 표시 좌표와 같은지 확인해야 한다. 같은 함정에 이미 한 번 빠졌다 (`29beadd`) — 회전 4방향 테스트 필수 |
| **벡터 그림** | 도형으로 그린 그림은 이미지가 아니라 `get_images()` 에 안 잡힌다. 이번 범위 밖 |
| **헛수고** | 13쪽은 그림 8개에 4.21초를 쓰고 **0줄**을 얻는다. 글자가 있는지는 돌려봐야 안다 |
| **겹침** | OCR 상자가 본문 위에 얹히면 클릭 대상이 모호해진다. 3.2-3 이 대부분 막지만 완전하지는 않다 |

## 5. 기각한 대안

- **항상 전체 OCR** — 그림 없는 페이지도 5초씩 낸다 (3·4쪽). 얻는 것이 없다.
- **그림마다 따로 OCR** — 원래 발상. 실측 88%, 14쪽 중 8쪽에서 오히려 느리다 (§2).
- **글자층 판정을 아예 없애고 늘 OCR** — 글자층이 OCR 보다 정확하다는 원칙
  ([extractor.py:4](app/core/extractor.py:4))을 깬다.

## 6. 열린 결정

1. **자동인가 버튼인가** — 페이지 표시마다 자동으로 2단계를 돌릴지, `그림 속 글자 찾기`
   버튼을 눌렀을 때만 돌릴지. 자동이면 그림 많은 문서에서 계속 OCR 이 돈다.
2. **상자 색을 나눌지** — 글자층 상자와 OCR 상자는 신뢰도가 다르고, 클릭하면 파괴
   목록에 들어간다. 같은 모양으로 보이는 게 정직한지.
3. **1% 임계값** — 실측으로 정한 값이다. 다른 문서에서 검증되지 않았다.

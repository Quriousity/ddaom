# 삭제 모드 — 설계 (2026-08-04)

> **상태: 설계 확정, 구현 전.** 대상 = `app/ui/*` 3개 파일. 코어(`redactor.py`) 수정 없음.

## 1. 문제

글자 인식 표시가 켜진 상태에서 박스를 클릭하면 **복사**만 된다
([pdf_view.py:234](app/ui/pdf_view.py#L234) → `textBoxClicked`).
필요한 건 **삭제(파괴)** 다. 그리고 지금의 파괴는 드래그 선택 **1개**를 즉시 저장하는
일회성 흐름이라([main_window.py:442](app/ui/main_window.py#L442)) 여러 곳을 지우려면
같은 문서를 몇 번씩 다시 저장해야 한다.

## 2. 확정된 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 조작 방식 | **삭제 모드 토글** (툴바 스위치 + 단축키 `D`) | 클릭 즉시 파괴는 되돌릴 수 없는 동작을 한 번의 실수로 실행시킨다 |
| 목록 위치 | **담은 텍스트 패널을 전환**해 삭제 목록으로 | 새 패널을 만들지 않는다 — 화면을 더 쪼개지 않는다 |
| 범위 | 페이지를 넘나들며 누적, **저장은 한 번에** | `redactor.redact()` 가 이미 `{page: [region,…]}` 를 받는다 |
| 취소 | 박스 재클릭 / 목록에서 `Del` / 전체 취소 | 실행 전까지는 무엇도 파괴되지 않는다 |
| 삭제 단위 | **줄 단위** (현행 스캔 박스 그대로) | 세밀한 건 드래그 영역으로 보완. 단어 단위는 다음 단계 |
| 복사 기능 | **유지** — 삭제 모드를 끄면 지금과 동일 | 클릭 한 번으로 복사는 이 도구의 핵심 UX 다 |

기각한 대안: `Shift+클릭=삭제`(발견 불가, 마우스 전용 사용자 배제),
`클릭=삭제로 완전 교체`(복사 UX 상실).

## 3. 데이터 모델

```python
@dataclass
class DeleteItem:
    page_no: int
    region: fitz.Rect | list[Point]   # PDF point — 박스=폴리곤, 드래그=Rect
    text: str                          # 목록 표시용 ("" 면 영역 항목)
    kind: str                          # "box" | "area"
    def key(self) -> tuple: ...        # 같은 박스 재클릭 = 취소 판정
```

`MainWindow.delete_items: list[DeleteItem]` 가 **유일한 진실**.
뷰는 표시만 하고 보관하지 않는다 (선택 상태를 PDF point 로만 보관하는 기존 규칙과 동일).

실행 시점에만 `{page_no: [region, …]}` 로 접어서 `redactor.redact()` 에 넘긴다.

## 4. UI

### 4.1 툴바

`글자 인식 표시` 스위치 옆에 **`삭제 모드`** 스위치 (danger 색, `theme.py` 의 `#danger`).

- 삭제 모드 ON → 박스 표시 강제 ON, 그동안 `T` 비활성 (박스가 안 보이면 삭제 모드가 무의미)
- 삭제 모드 OFF 로 되돌려도 **담긴 목록은 유지** — 실수로 꺼도 날아가지 않는다

### 4.2 담은 텍스트 패널 → `QStackedWidget` 2장

| | 일반 모드 (page 0) | 삭제 모드 (page 1) |
|---|---|---|
| 제목 | 담은 텍스트 | **삭제 목록 (N건)** |
| 본문 | `QPlainTextEdit` (현행 그대로) | `QListWidget` |
| 항목 | — | `p3 · 홍길동 010-1234-…` / `p3 · 영역 120×36pt` |
| 버튼 | 전체 복사 / 지우기 | 선택 취소(`Del`) / 전체 취소 / **삭제 실행 → 저장** |

- 목록 항목 클릭 → 해당 페이지로 점프 + 그 영역 강조
- 삭제 모드를 켜면 패널이 접혀 있어도 자동으로 펴진다 (`a_tray_toggle`)

### 4.3 뷰 오버레이

- 담긴 영역 = 빨간 반투명 사각형, `zValue=5` (`TextBoxItem` 3 아래, 선택 10 위)
  — [selection.py:38](app/ui/selection.py#L38) 주석이 예고하던 그 자리다
- 같은 박스 재클릭 = 취소
- `Esc` = 드래그 선택 해제 (**목록은 건드리지 않는다**)
- 삭제 모드에서 드래그 완료 = 영역 항목으로 **바로 담김** (파괴가 아니라 담기이므로 되돌릴 수 있다)

## 5. 파일별 변경

| 파일 | 변경 | 규모 |
|---|---|---|
| `app/core/redactor.py` | **없음** — 다중 페이지·다중 영역을 이미 지원 | 0 |
| `app/ui/selection.py` | `DeleteRegionItem` 추가, `TextBoxItem` 이 원본 PDF 폴리곤을 보관 | +25 |
| `app/ui/pdf_view.py` | `delete_mode`, `set_delete_regions()`, 클릭 분기, 오버레이 재그리기 | +40 |
| `app/ui/main_window.py` | `DeleteItem`, 스택 트레이, 목록 UI, `execute_deletions()` | +140 |
| `app/config.py` | 오버레이 색상 2개 | +2 |

유일한 구조 손질: `TextBoxItem` 이 지금 씬 좌표만 들고 있어
([selection.py:33](app/ui/selection.py#L33)) 클릭해도 PDF 좌표를 복원할 수 없다.
생성자에 `poly_pt` 를 추가한다.

## 6. 실행 흐름

```
삭제 실행
  → 확인 다이얼로그 ("N건 · M개 페이지를 파괴합니다. 되돌릴 수 없습니다.")
  → 저장 경로  (_next_redacted_path — 이미지 원본이면 .png)
  → 원본 덮어쓰기 차단 (현행 검사 유지)
  → 백그라운드 워커: redactor.redact(src, {page: [...]}, dst, password=…)
  → 검증 리포트 (잔여 텍스트 / 메타데이터 / XMP — 현행 그대로)
  → 성공: 목록 비우기 + "저장본을 열까요?" → open_file(dst) (박스도 자동 재스캔)
  → 실패: 목록 유지 (다시 시도할 수 있어야 한다)
```

단축키: `D` = 삭제 모드, `Del` = 목록에서 항목 취소, `Ctrl+Shift+D` = 삭제 실행.

## 7. 구현 전 확인해야 할 위험

1. **줄 bbox 세로 겹침 (중요)**
   행간 좁은 문서에서 줄 bbox 가 위/아래 줄에 닿으면 `apply_redactions` 가
   인접 줄 글자까지 지운다. **검증 리포트는 이걸 못 잡는다** — 지정 영역 *안의* 잔여만 본다.
   → 완화: `scan_page_boxes` 의 글자층 박스를 line bbox 대신 span bbox 합집합으로 만들고
   세로 0.5pt 인셋. 실제 문서로 육안 검증 필요.

2. **기울어진 스캔본**
   회전된 OCR 폴리곤이 bbox 로 확대되어([redactor.py:48](app/core/redactor.py#L48))
   옆 글자를 먹는다. 이번 범위 밖 — `add_redact_annot(quad=…)` 지원으로 별도 해결.

3. **미스캔 페이지엔 박스가 없다**
   페이지를 한 번 봐야 스캔된다 (`SCAN_ON_PAGE_SHOW`). "문서 전역"이 곧
   "전 페이지 자동"은 아니다. 문서 전체 사전 스캔은 다음 단계(§9).

4. **OCR 재현율 종속**
   인식되지 않은 글자는 박스가 없으니 삭제 대상에서 빠진다. "지운 줄 알았는데 남아 있음"이
   최악의 실패다 → **드래그 영역 파괴는 절대 없애지 않고 병행한다.**

5. **목록 유실 방지**
   다른 문서 열기 / 앱 종료 시 미실행 목록이 있으면 경고 다이얼로그.

## 8. 스캔본에서 지운 자리

픽셀 파괴라 흰 사각형이 남는다 (`REDACT_FILL_COLOR = (1,1,1)`).
자연스럽게 메우는 것(인페인팅)은 범위 밖이다.
글자층 PDF 는 배경이 유지되고 글자만 사라진다.

## 9. 다음 단계 (이번 범위 아님)

- **문서 전체 사전 스캔** — 전 페이지 박스를 미리 확보해야 텍스트 기반 일괄 삭제가 가능해진다
- **텍스트 일괄 삭제** — "이 이름 전 페이지에서 모두", 주민번호·전화번호·계좌번호 정규식 자동 탐지
- **단어 단위 삭제** — 글자층 문서에 한해 `get_text("words")` 박스 병행
- **진짜 미리보기(WYSIWYG)** — 메모리에서 리댁션 → `tobytes()` → 재렌더.
  조작마다 문서 전체 재작성이라 큰 PDF 는 느리다. §4.3 오버레이로 충분한지 먼저 판단한다
- **quad 리댁션** — 위험 2번 해결

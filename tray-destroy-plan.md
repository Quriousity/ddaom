# 담은 것을 파괴한다 — 설계와 구현 기록 (2026-08-04)

> **상태: 구현 완료.** 모드 없이, 담은 목록 하나가 복사 대상이자 파괴 대상이다.
> 대상 = `app/ui/*`, `app/core/*`. 테스트 `tests/test_ui_smoke.py` 79건.
>
> 이 문서는 폐기된 `delete-mode-plan.md`(삭제 모드 설계)를 흡수했다. 거기서 확정했던
> 위험 목록과 기각 이유는 §7·§8 에 그대로 살아 있다.

## 1. 문제와 결론

인식된 글자 상자를 클릭하면 **복사**만 됐고, 파괴는 드래그 하나를 즉시 저장하는
일회성 흐름이었다. 여러 곳을 지우려면 같은 문서를 몇 번씩 다시 저장해야 했다.

한 번은 **삭제 모드**(토글 스위치 + 별도 목록)로 풀었다. 그러나 그것은
[main_window.py](app/ui/main_window.py) 첫머리에 적힌 원칙을 정면으로 깼다:

> 모드 전환·리댁션 대기 목록은 없다.

**결론: 모드를 없애고 담은 목록 하나로 합친다.** 박스를 클릭하는 행위의 뜻은 언제나
"이 항목을 골랐다" 하나다. 고른 뒤에 복사할지 파괴할지는 나중에 정하면 된다.
모드는 그 결정을 클릭 *전에* 강요했다 — 순서가 거꾸로였다.

## 2. 확정된 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 모드 | **없다** | 원칙 복원. 스위치 종속·이탈 경고·패널 전환이 통째로 사라진다 |
| 담기 | 글자 상자 클릭 = **복사 + 담기**, 드래그 후 `영역 파괴` = 담기 | 클릭 한 번이 두 일을 한다. 복사는 이 도구의 핵심 UX 라 유지 |
| 목록의 뜻 | **담긴 것이 곧 파괴 대상** | 규칙이 하나뿐이라 설명이 필요 없다 |
| 빼기 | 행의 **`✕`** | 행 자체가 사라진다 — 복사에서도 빠진다 |
| 중복 | 같은 자리를 또 클릭해도 **한 번만 담긴다** (복사는 다시 된다) | 토글로 하면 '복사하려고 또 눌렀다가 목록에서 빠지는' 사고가 난다 |
| 실행 | 툴바 **`이대로 저장하기`** — 원본 형식으로 (PDF→PDF, 이미지→PNG) | 파괴의 유일한 출구. `영역 파괴`(담기)와 구분선으로 갈라 놓는다 |
| 단축키 | 담기 `Ctrl+D`, **실행은 없음** | 되돌릴 수 있는 것에만 단축키를 준다 |
| 글자 편집 | 행 안에서 직접 (`box` 항목만) | OCR 오타 수정. 편집은 복사에만 영향, 파괴는 좌표 기준 |
| 복사 | `클립보드에 복사`(Ctrl+C)는 **담지 않는다** | 내보내기와 담기는 다른 일이다. 담으면 파괴 대상이 된다 |

기각한 대안:
`⌥클릭=파괴담기`(발견 불가, 마우스 전용 사용자 배제),
`Shift+클릭=삭제`(같은 이유),
`클릭=삭제로 완전 교체`(복사 UX 상실),
`행마다 파괴 체크박스`(열이 늘어 패널이 복잡해진다),
`행 선택으로 복사/파괴를 가르기`(목록 = 파괴 대상이라는 규칙이 흐려진다).

## 3. 데이터 모델

```python
@dataclass
class TrayItem:
    page_no: int
    region: fitz.Rect | list[Point]   # PDF point — 박스=폴리곤, 드래그=Rect
    text: str                          # 표시·복사용. 고쳐도 파괴는 region 기준
    kind: str                          # "box" | "area"
```

`MainWindow.tray_items` 가 **유일한 진실**. 뷰는 표시만 하고 보관하지 않는다.
실행 시점에만 `{page_no: [region, …]}` 로 접어 `redactor.redact()` 에 넘긴다.

## 4. UI

### 4.1 툴바

```
열기 │ 클립보드에 복사  이미지로 저장  이대로 저장하기 │ 영역 파괴 │ 글자 인식 표시 ●─ │ …
      └────────── 내보내기 ──────────┘             └ 담기 ┘
```

`영역 파괴`는 내보내기가 아니라 담기다 — **구분선으로 성격이 다름을 보인다.**
`이대로 저장하기`는 danger 색(hover 시 빨강), 담은 게 없으면 비활성.

### 4.2 담은 목록 패널

행 = `[p1] [고칠 수 있는 글자] [✕]` (`widgets.TrayRow`).
비어 있으면 안내문이 대신 보인다. 아래에 `전체 복사` / `전체 비우기`.

- 행의 글자를 클릭하면 **그 페이지로 점프**하고 해당 영역이 진하게 강조된다
- `영역` 항목은 글자가 없으므로 읽기 전용 (`영역 120×36pt`)

### 4.3 뷰 오버레이

담긴 영역 = 빨간 반투명 사각형, `zValue=5` (`TextBoxItem` 3 위, 선택 10 아래).
마우스를 받지 않아 아래 박스를 계속 클릭할 수 있다.

## 5. 실행 흐름

```
이대로 저장하기
  → 확인 다이얼로그 (N건 · M개 페이지 + 앞 3건 미리보기, "되돌릴 수 없습니다")
  → 저장 경로 (_next_redacted_path — 이미지 원본이면 .png)
  → _dst_ok: 원본 덮어쓰기 차단 + 쓰기 가능 여부 (redactor.save_blocker)
  → 백그라운드 워커: redactor.redact(src, {page: [...]}, dst, password=…)
  → 검증 리포트 (잔여 텍스트 + 파괴 영역 픽셀 + 메타데이터 + XMP)
  → 성공: 목록 비우기 + "저장본을 열까요?"
  → 실패: 목록 유지 (다시 시도할 수 있어야 한다)
```

## 6. 문서를 바꾸면

담긴 좌표는 이전 문서의 것이다. **문서를 바꾸거나 앱을 닫을 때 담긴 게 있으면
경고하고, 확인하면 비운다** (`_confirm_discard`).

> ⚠ 이것은 의도한 회귀다. 예전 트레이는 순수 텍스트라 여러 문서를 돌며 값을 모을 수
> 있었다. 목록이 좌표를 들면서 그 흐름은 포기했다 — 이전 문서 좌표로 현재 문서를
> 지우는 사고가 훨씬 위험하기 때문이다.

## 7. 남은 위험과 그에 대한 결정 (삭제 모드 설계에서 승계)

1. **줄 bbox 세로 겹침 — 완화하지 않는다**
   행간 좁은 문서에서 줄 bbox 가 위/아래 줄에 닿으면 인접 줄 글자까지 지워진다.
   **검증 리포트는 이걸 못 잡는다** — 지정 영역 *안의* 잔여만 본다.
   현행 스캔 박스를 그대로 쓴다. 실제 문서에서 문제가 쌓이면 그때 다룬다.

2. **기울어진 스캔본 — bbox 로 넘치게 지운다**
   회전된 OCR 폴리곤이 bbox 로 확대되어([redactor.py](app/core/redactor.py)
   `_to_rect`) 옆 글자를 먹는다. 위험 1 과 같은 종류라 같은 판단을 적용한다.

   > **quad 리댁션은 해결책이 아니다 (2026-08-04 실측, PyMuPDF 1.27.2.3).**
   > `add_redact_annot()` 에 회전 quad 를 넘겨도 파괴는 **quad 의 bounding box** 로
   > 수행된다. 45° quad vs 그 bbox — 이미지 픽셀 40000 대 40000 (비율 1.0),
   > 글자층도 quad 바깥 모서리 텍스트까지 삭제됐다. 정말 '범위 안만' 지우려면
   > 폴리곤을 얇은 축정렬 사각형으로 쪼개야 하고, 이는 코어 변경을 부른다 — 범위 밖.

3. **미스캔 페이지엔 박스가 없다 — 사용자가 인지할 사항**
   페이지를 한 번 봐야 스캔된다 (`SCAN_ON_PAGE_SHOW`). "문서 전역"이 곧
   "전 페이지 자동"은 아니다.

4. **OCR 재현율 종속 — 드래그 영역으로 보완**
   인식되지 않은 글자는 박스가 없어 클릭할 수 없다. "지운 줄 알았는데 남아 있음"이
   최악의 실패다 → `영역 파괴`(드래그 담기)를 **몇 번이든** 반복할 수 있게 유지한다.

5. **스캔본에서 지운 자리**
   픽셀 파괴라 흰 사각형이 남는다 (`REDACT_FILL_COLOR = (1,1,1)`).
   인페인팅은 범위 밖. 글자층 PDF 는 배경이 유지되고 글자만 사라진다.

## 8. 파괴가 조용히 실패하던 경로 (2026-08-04 감사에서 발견·수정)

**증상**: 저장은 됐는데 아무것도 지워지지 않았고, 리포트는 `OK` 였다.

**원인**: `coords.clamp_rect` 가 자른 뒤 `normalize()` 를 불렀다. 페이지 밖 선택은
자르고 나면 `x0 > x1` 이 되는데 `normalize()` 가 좌표를 맞바꿔 '페이지 안의 엉뚱한
사각형'으로 되살렸다. 그 사각형은 아무것도 지우지 않는데, 검증은 *지정 영역 안의*
잔여만 보므로 통과했다. **페이지 옆 여백을 드래그하면 실제로 재현됐다.**

**막은 방법 (여러 겹)**

| 겹 | 내용 |
|---|---|
| `coords.clamp_rect` | 겹치는 곳이 없으면 빈 사각형. 자른 뒤 `normalize()` 금지 |
| `redactor._plan()` | 못 지울 선택이 하나라도 있으면 **문서에 손대기 전에 중단**. 반만 지워진 파일이 더 위험하다 |
| `apply_redactions` 반환값 | `False`(무동작)면 저장하지 않고 중단 |
| `_verify` 픽셀 검사 | 글자층 없는 스캔본은 텍스트 검사가 **언제나 통과**한다. 파괴 영역이 fill 색으로 덮였는지도 본다 |

**정상으로 확인된 경로**: 글자층 PDF, 스캔본, 투명 OCR 텍스트층, 부분 겹침,
회전 페이지(`/Rotate 90`), 벡터 그림, 공유 XObject(한쪽만 지워도 다른 사본 보존),
SMask 투명 이미지, 꺼진 OCG 숨은 레이어, 암호 PDF, CropBox 원점이 0이 아닌 문서.

## 9. 열 때 능력 판정 (`app/core/capability.py`)

| 갈래 | 동작 |
|---|---|
| ① 못 읽는다 | 경고 후 **열지 않는다** (손상·미지원·암호 실패·페이지 0·렌더 실패) |
| ② 읽지만 못 지운다 | 열되 경고 + `이대로 저장하기` 잠금. 복사·이미지 저장은 된다 |
| ③ 다 된다 | **경고 없이 조용히** |

②의 대부분은 결과를 저장할 폴더가 읽기 전용인 경우다. 내가 소유자면 경고에
**`이 폴더에 쓰기 허용`** 버튼이 붙는다 — `u+w` 만 켜고(재귀 없음, 다른 권한 불변)
능력을 다시 판정해 잠금을 푼다.

저장 실패는 두 겹으로 막는다: 열 때 미리(`capability.probe`), 저장 직전에 또
(`_dst_ok`). 그래도 실패하면 `redactor._save` 가 PyMuPDF 의 메시지 없는 예외를
읽을 수 있는 `OSError` 로 바꾼다.

## 10. 다음 단계 (범위 밖)

- **문서 전체 사전 스캔** — 전 페이지 박스를 미리 확보해야 텍스트 기반 일괄 삭제가 된다
- **텍스트 일괄 담기** — "이 이름 전 페이지에서 모두", 주민번호·전화번호·계좌번호 정규식 자동 탐지
- **단어 단위** — 글자층 문서에 한해 `get_text("words")` 박스 병행
- **폴리곤 슬라이스 파괴** — 위험 2 를 정말로 풀려면. quad 는 안 되는 것으로 확인됐다
- **진짜 미리보기(WYSIWYG)** — 메모리에서 리댁션 → 재렌더. 큰 PDF 는 느리다.
  §4.3 오버레이로 충분한지 먼저 판단한다
